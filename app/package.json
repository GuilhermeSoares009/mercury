{
  "name": "mercury",
  "version": "0.1.0",
  "description": "Mercury — AI-powered job search companion. CLI + local dashboard for LinkedIn job search, recruiter tracking, profile scoring, and resume tailoring.",
  "type": "module",
  "bin": {
    "mercury": "./dist/mercury"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build": "bun run build:web && bun run embed && bun run build:bin",
    "build:web": "cd web && bun install && bun run build",
    "embed": "bun run scripts/embed-assets.ts",
    "build:bin": "bun build src/cli/index.ts --compile --outfile dist/mercury",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
